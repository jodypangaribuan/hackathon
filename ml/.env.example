# Local path to the organizer dataset directory.
SIPATURE_DATASET_DIR=../Datasets

# Optional artifact root override. Defaults to ml/artifacts.
SIPATURE_ARTIFACT_DIR=./artifacts

# Never place access tokens in committed files.
HF_HOME=./.cache/huggingface
